@model IEnumerable<MyTinyCollege.Models.Course>

@{
    ViewBag.Title = "Available Courses";
}

<h2>Available Courses</h2>
@using (Html.BeginForm())
{
    <div class="form-inline">
        <div class="form-group">
            Select Department:
            @Html.DropDownList("SelectedDepartment", null, "All",
           new
           {
               onchange = "$(this).closest('form').submit();",
               @class = "form-control input-sm"
           })
            <input type="submit" class="btn btn-sm btn-default" id="filter" name="filter" />
        </div>
    </div>
}
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            @*<h2>@Html.DisplayFor(model => item.Title)</h2>*@
            <h2>@item.Title</h2>
            <p>Description would go here..</p>
            <p>@Html.DisplayFor(model => item.Credits) Credits </p>
            @Html.ActionLink("Details", "Details", new { id = item.CourseID }, new
       {
           @class="btn btn-primary"
       }
           )
        </div>
    }
</div>



@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Department.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Credits)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Credits)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CourseID }) |
                @Html.ActionLink("Details", "Details", new { id=item.CourseID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CourseID })
            </td>
        </tr>
    }

    </table>*@
